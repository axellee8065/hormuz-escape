{
  "name": "hormuz-escape",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "HORMUZ ESCAPE — 9:16 세로형 Canvas 2D 랭킹 게임",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port $PORT --host 0.0.0.0",
    "start": "npx serve dist -l $PORT"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "vite": "^5.4.0"
  }
}
