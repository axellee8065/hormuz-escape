# Railway builds via Nixpacks automatically. This file pins the build pipeline.
# Flow: install → build → start (serves dist/ on $PORT).

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve dist -l $PORT"
